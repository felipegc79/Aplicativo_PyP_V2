import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tikkaconsultores.gestionpyp',
  appName: 'Tikka Gestion PyP',
  webDir: 'build'
};

export default config;
